# Specifications

The blueprint — a human reads the essays, an AI reads the specs. These ten chapters contain the complete technical specification for suppr, precise enough that an AI can build the app from them alone.
